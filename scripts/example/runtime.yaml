api_version: 1.1
configs:
  conda:
    channels:
      - conda-forge
dependencies:
  - pip:
      - numpy
  - requirements-sw-lock.txt
  - commands:
      - apt-get install -y libgl1
      - touch /tmp/runtime-command-run.flag
environment:
  arch: noarch
  os: ubuntu:20.04
mode: venv
name: simple-test
