name: Server Unit test Report

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'server/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required" '
