version: 1.0
name: mnist

model:
  - models/mnist_cnn.pt

config:
  - config/hyperparam.json

run:
  runtime: python3.8
  ppl: mnist.ppl:MNISTInference
  pip_req: requirements.txt

desc: mnist by pytorch

tag:
  - classify