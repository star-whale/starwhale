version: 1.0
name: mnist

model:
  - models/mnist_cnn.pt

config:
  - config/hyperparam.json

run:
  runtime: python3.8
  ppl: ppl.py
  pip_req: requirements.txt
  args: --predictor

desc: mnist by pytorch

tag:
  - classify