/// <reference types="react" />
import { WidgetProps } from '@rjsf/utils';
declare const SelectWidget: {
    ({ autofocus, disabled, formContext, id, multiple, onBlur, onChange, onFocus, options, placeholder, readonly, schema, value, }: WidgetProps): JSX.Element;
    defaultProps: {
        formContext: {};
    };
};
export default SelectWidget;
